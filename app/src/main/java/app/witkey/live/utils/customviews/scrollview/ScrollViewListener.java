package app.witkey.live.utils.customviews.scrollview;

/**
 * created by developer on 10/26/2017.
 */

public interface ScrollViewListener {
    void onScrollEnded(EndLessScrollView scrollView, int x, int y, int oldx, int oldy);
}